{
  "name": "@winkintel/sendgrid-skills",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Skills for sending email via SendGrid v3 Web API",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p .",
    "test": "vitest run",
    "lint": "eslint src --ext .ts,.tsx || echo 'eslint not configured'",
    "check": "pnpm build && pnpm test"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
